import React from 'react';
import { CTickerList } from '../tickerList/TickersList';

const FollowList = () => {
    return (
        <CTickerList mode='follow-board'/>
    );
};

export { FollowList };